# תיקון מערכת התזרימים - סיכום מעמיק

## הבעיה שהתגלתה

1. **רשימת תזרימים קבועה במודל** - תפריט התזרימים היה קווד בצורה קבועה (1-5) במקום לקרוא מה-DB
2. **עמודת תזרים לא מתעדכנת** - כשמשנים קטגוריה, עמודת התזרים בטבלה לא התעדכנה
3. **חוסר סנכרון** - הרשומות לא תמיד סונכרנו עם התזרים של הקטגוריה שלהן

## האדריכלות הנכונה

### מבנה הקשר בין הטבלאות:
```
flows (תזרימים)
  ↓ (flow_id)
categories (קטגוריות)
  ↓ (category_id) 
transactions (רשומות)
  ↓ (flow_id - עותק לביצועים)
```

**עקרון חשוב:** 
- כל קטגוריה משויכת לתזרים אחד בלבד (categories.flow_id)
- כל רשומה שייכת לקטגוריה (transactions.category_id)
- לרשומה יש גם flow_id משלה (עותק מהירות גישה) שצריך להתעדכן אוטומטית

## השינויים שבוצעו

### 1. ✅ API חדש לשליפת תזרימים (app.py)
```python
@app.route('/api/flows')
def api_flows():
    """שליפת רשימת תזרימים פעילים מה-DB"""
```

### 2. ✅ טעינה דינמית של תזרימים (list.html)
- הוספת משתנה `allFlows`
- פונקציה `loadFlows()` שטוענת תזרימים מה-API
- עדכון `openAddCategoryModal()` למלא תזרימים דינמית

### 3. ✅ עדכון אוטומטי של flow_id ברשומה (transactions.py)
כשמשנים קטגוריה של רשומה:
1. שולף את flow_id מהקטגוריה החדשה
2. מעדכן גם category_id וגם flow_id ברשומה
3. מחזיר את שם התזרים החדש לעדכון התצוגה

### 4. ✅ עדכון תצוגת התזרים ב-JavaScript
- אחרי שינוי קטגוריה - מעדכן גם את התא של התזרים
- אחרי יצירת קטגוריה חדשה - מעדכן גם את התא של התזרים

### 5. ✅ הוספת עמודת תזרים לטבלת הרשומות
- עמודה חדשה אחרי "קטגוריה"
- מציגה את שם התזרים של הרשומה

## סקריפטים חדשים

### migrate_update_transaction_flows.py
עדכון חד-פעמי לכל הרשומות הקיימות:
```bash
python migrate_update_transaction_flows.py
```
מעדכן את flow_id של כל הרשומות בהתאם לקטגוריה שלהן.

### check_database_structure.py
בדיקת תקינות המערכת:
```bash
python check_database_structure.py
```
בודק:
- האם העמודות flow_id קיימות
- כמה קטגוריות ללא תזרים
- כמה רשומות צריכות עדכון

## הוראות שימוש

### התקנה ראשונית:
1. העתק את הפרויקט המעודכן
2. הרץ: `python check_database_structure.py` - לבדיקת מצב
3. הרץ: `python migrate_update_transaction_flows.py` - לעדכון רשומות קיימות
4. הפעל את השרת: `python start.py`

### בדיקה:
1. פתח דף רשומות
2. לחץ דבל-קליק על קטגוריה
3. בחר "הוספת קטגוריה חדשה"
4. וודא שרשימת התזרימים מגיעה מה-DB (לא קבועה 1-5)
5. שנה קטגוריה ובדוק שעמודת התזרים מתעדכנת

## תועלות השינוי

1. **גמישות** - אפשר להוסיף/לערוך תזרימים בלי לשנות קוד
2. **עקביות** - התזרים של הרשומה תמיד תואם לקטגוריה
3. **ביצועים** - flow_id ברשומה מאפשר שאילתות מהירות
4. **תחזוקה** - קל יותר להבין ולתחזק

## מבנה הטבלאות (רלוונטי)

### categories
- id, name, parent_id, level, full_path
- **flow_id** (INTEGER) ← חיבור לתזרים
- description, icon, color, is_system
- created_at, updated_at

### transactions  
- id, transaction_date, description, amount
- **category_id** (INTEGER) ← חיבור לקטגוריה
- **flow_id** (INTEGER) ← עותק מהיר, צריך להתעדכן אוטומטית
- account_id, is_installment, is_excluded
- created_at, updated_at

---
תיקון בוצע על ידי Claude | נובמבר 2025
